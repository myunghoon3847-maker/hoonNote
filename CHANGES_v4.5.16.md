# 훈노트 v4.5.16 변경 사항

## 화면 구조

- 실제 사용되지 않던 `settingsView` 별도 화면을 HTML에서 제거했습니다.
- 계정 관리와 앱 설치 기능을 메뉴 내부 설정 화면으로 단일화했습니다.
- 앱 설치 버튼의 ID와 PWA 연결은 유지하여 설치 기능이 끊기지 않도록 했습니다.

## JavaScript

- 존재하지 않는 DOM 요소를 조회하던 변수와 선택적 이벤트 코드를 제거했습니다.
- 제거 대상: 구 설정 화면, 옛 메인 카테고리 관리 버튼, 옛 메모 작성 뒤로가기 버튼, 삭제된 휴지통 비우기 메뉴 버튼, 사용하지 않는 할 일 접기 처리.
- 앱 최상위 화면을 `메모·할 일·휴지통` 세 종류로 정리했습니다.
- 과거 `settings` 화면 이력은 메모 화면으로 안전하게 정규화됩니다.

## CSS

- 현재 HTML과 JavaScript 어디에서도 사용하지 않는 선택자 79개를 제거했습니다.
- 동일한 선언을 가진 완전 중복 규칙 2개를 제거했습니다.
- 제거 범위에는 과거 로고, 카테고리 더보기, 과거 설정 아코디언, 독립 메모 작성 화면, 별도 설정 화면 스타일이 포함됩니다.
- 현재 로고, 메뉴 설정, 인라인 메모 작성, 오류 안내 스타일은 유지했습니다.

## 프로젝트 정리

- 과거 버전별 변경·검수·배포 체크 문서 누적을 배포본에서 제거했습니다.
- v4.5.14에서 운영 DB에 적용·확인한 RLS 감사 및 최소 권한 SQL 06~11번을 `supabase/sql/`에 포함했습니다.
- 리소스 캐시 버전을 `v=467`, 앱 버전을 `v4.5.16`으로 갱신했습니다.
